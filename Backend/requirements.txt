
pdfplumber>=0.11
pypdfium2>=4.0
python-docx>=1.1
olefile>=0.47
pandas>=2.0
openpyxl>=3.1
psycopg2-binary>=2.9
python-dotenv>=1.0
sqlalchemy>=2.0
phonenumbers>=8.13
pycountry>=23.12
pytesseract>=0.3.10
fastapi>=0.104.0
uvicorn>=0.24.0
python-multipart>=0.0.9
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0
passlib[bcrypt]>=1.7.4

# NER-based name validation (validate_name in validation_layer.py)
# Required for consistent name parsing across environments
spacy>=3.7

# GLiNER: zero-shot NER for resume field extraction (name, title, location, skills)
# Significantly improves accuracy over spaCy en_core_web_sm on resume domain
# Model (~500MB) is auto-downloaded on first use from HuggingFace
gliner>=0.2.0

# Required by GLiNER
transformers>=4.35.0
torch>=2.0.0

# OpenAI SDK — used for Groq and Ollama (OpenAI-compatible) LLM calls
openai>=1.0

# Optional: Google Drive folder sync (RESUME_INPUT_MODE=gdrive)
google-api-python-client>=2.120
google-auth>=2.25
google-auth-oauthlib>=1.2

# Optional: LLM-powered extraction layer (set LLM_EXTRACT_ENABLED=true to activate)
# Choose ONE depending on your LLM provider:
#   OpenAI / Groq / any OpenAI-compatible API:
# openai>=1.0
#   Anthropic Claude:
# anthropic>=0.20
